{
  "name": "ci-loop",
  "version": "1.0.0",
  "description": "CI Loop 演示项目 — 验证 GitHub CI + Claude Code 自动修复循环",
  "main": "src/index.js",
  "scripts": {
    "test": "jest --verbose",
    "test:ci": "jest --verbose --json --outputFile=test-results.json",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "build": "node -e \"console.log('Build OK')\""
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "eslint": "^8.57.0"
  }
}
