// src/index.js - 主入口

const calculator = require('./calculator');
const validator = require('./validator');
const formatter = require('./formatter');

module.exports = { calculator, validator, formatter };
